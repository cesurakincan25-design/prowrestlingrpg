name = "ring-rp-ai"
main = "worker.js"
compatibility_date = "2026-09-01"

[vars]
# GitHub Pages adresini ve yerel test adresini yaz (virgülle)
ALLOWED_ORIGINS = "https://cesurakincan25-design.github.io,http://localhost:5500,http://127.0.0.1:5500"
DEFAULT_MODEL = "gemini-3.6-flash"
RATE_PER_MIN = "40"

# Secret'lar buraya YAZILMAZ. Terminalden:
#   npx wrangler secret put GEMINI_KEY
#   npx wrangler secret put APP_TOKEN     (isteğe bağlı)
